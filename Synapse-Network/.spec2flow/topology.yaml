topology:
  services:
    - name: app
      kind: backend
      critical: true
      healthChecks:
        - type: command
          target: echo "ok"
          timeoutSeconds: 30
  startupOrder:
    - app
  workflowRoutes:
    - name: default
      entryServices:
        - app
      verifyCommands:
        - echo "verify"
      artifactTargets:
        - .spec2flow/generated/execution/default
      requirementSignals:
        summary: Default delivery route for synapse-network.
        keywords:
          - feature
          - fix
          - implement
          - build

# Auto-generated by Spec2Flow. Customise services and routes to match your project.
