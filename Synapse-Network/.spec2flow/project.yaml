spec2flow:
  project:
    name: synapse-network
    type: single-app
    defaultBranch: main
    owner: synapse-network
  docs:
    requirements:
      - .spec2flow/project.yaml
    architecture:
      - .spec2flow/project.yaml
  services:
    app:
      path: .
      type: backend
      start: echo "ready"
      test: echo "ok"
  infrastructure:
    bootstrap: echo "bootstrap"
  tests:
    build: echo "build"
  artifacts:
    baseDir: .spec2flow/generated
    executionDir: .spec2flow/generated/execution
    bugsDir: .spec2flow/generated/bugs

# Auto-generated by Spec2Flow for project: Synapse-Network
# Customise start/test commands to match your actual build toolchain.
