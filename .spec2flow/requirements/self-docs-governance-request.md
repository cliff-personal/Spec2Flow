# Self Docs Governance Request

Goal: verify that Spec2Flow can route a repository-governance request against its own repository.

Requirement:
- tighten active documentation navigation
- keep historical plans out of active guidance
- move repeated documentation governance rules into the docs validator
- update only documentation governance surfaces and their validator coverage

Expected output:
- the docs-governance route should be selected first
- the requirement should not require schema-only or runtime-only work unless the planner finds a real dependency